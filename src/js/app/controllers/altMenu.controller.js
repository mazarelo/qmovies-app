/* login */
myApp.controller("AltMenuController" , function( $scope , $routeParams ) {
  const self = this;
  self.typesOfSearch = {
    active:'',
    options:[
      {name:"Test", value:"that"}
    ]
  }

});
